# Gmail SMTP Configuration
# Your Gmail address (the email that will send the onboarding emails)
GMAIL_USER=youremail@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# To get this:
# 1. Enable 2FA on your Google account (REQUIRED)
# 2. Go to Google Account > Security > App passwords
# 3. Generate a new app password for "Mail"
# 4. Use the 16-character password (Google generates this automatically)
#    Example: "abcd efgh ijkl mnop" or "abcdefghijklmnop"
GMAIL_PASS=your16characterapppassword

# Slack Bot Configuration
# Your Slack bot token (starts with xoxb-)
# Get this from: Slack App Dashboard > OAuth & Permissions > Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Your Slack app's signing secret (found in Slack app settings)
# Get this from: Slack App Dashboard > Basic Information > Signing Secret
SLACK_SIGNING_SECRET=your_slack_signing_secret_here

# Server Configuration
# Port for the Flask app (Render will auto-assign if not specified)
PORT=5000
